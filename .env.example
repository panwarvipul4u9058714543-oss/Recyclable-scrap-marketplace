# SQLite database location (relative to prisma/ dir uses file: prefix)
DATABASE_URL="file:./dev.db"

# Local/dev + test convenience ONLY. When set to "1", the mocked phone
# verification returns the one-time code in the API response so the flow can be
# used without a real SMS provider. NEVER set this in production.
RSM_EXPOSE_OTP="1"
